#include<bits/stdc++.h>
using namespace std;
ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
int main()
{
    1.无参(默认)构造：string str();
    2.字符串构造:string str(const char* s)
    3.string拷贝构造(涉及深拷贝)：string(const string& s)
    
    4.部分字符串初始化字符串：
    string s1(s0, begin, len);//截取s0的begin位置开始，len长度的字符串
    string s1(s0, begin);//字符串str中从下标begin开始到字符串结束的位置作为字符串初值
    
    5.字符数组初始化：
    char s[10]="qasa";
    string str(s);
    
    6.重复字符初始化： string str(12,'A');
    }
    实例测试用：
    int main()
{
    char s1("axax");
    string str(s1);
    string str1(str,1,3);
    string str2(str,2);
    string str3(10,'a');
    char s[2]="aa";
    string(s);
    return 0;
}
输出：
c_str(),返回一个指向以空字符结尾的C风格字符串(const char*类型)
输出时。将string转换为C风格的字符串输出
#include<bits/stdc++.h>
using namespace std;
ios::sync_with_stdio(0), cin.tie(0), cout.tie(0);
int main()
{
    char s1("axax");
    printf("%s",s1.c_str())
    return 0;
}
